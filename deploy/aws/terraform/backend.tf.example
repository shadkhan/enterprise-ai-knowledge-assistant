# Optional remote state backend.
# Copy this file to backend.tf after creating the state bucket and lock table.
#
# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "enterprise-ai-knowledge-assistant/aws/dev/terraform.tfstate"
#     region         = "us-east-1"
#     dynamodb_table = "terraform-state-locks"
#     encrypt        = true
#   }
# }
